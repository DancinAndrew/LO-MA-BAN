# Backend API base URL (no trailing slash). Used by preview, scan, persuade.
# Leave unset for local dev (http://localhost:8000).
VITE_SCOUTNET_API_URL=https://lo-ma-ban-production-9fb5.up.railway.app

# Optional: preview path (default /api/preview-url)
# VITE_SCOUTNET_PREVIEW_PATH=/api/preview-url
